# @vlue/web

VLUE 슈퍼앱 웹 클라이언트 (Vite + React).

- 개발: `npm run dev` (루트에서 `npm run web:dev`)
- 빌드 산출물: `web/dist`
- API 프록시: `/api` → `http://127.0.0.1:8788`
- 공통 BM: `@vlue/shared` (`packages/shared`)

프로덕션 ENV: `web/.env.production` (`VITE_API_URL`)
