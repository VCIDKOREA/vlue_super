# Railway Variables — @vlue/api 서비스에 아래 키를 복사·값만 채워 넣으세요.
# (이 파일은 Git에 포함됩니다. 실제 비밀값은 Railway에만 저장.)

# === 필수 (4) ===
# Supabase ywhjhdpecwvaujiagaln — 인프라마스터 DATABASE_URL 전체 붙여넣기 (Railway Postgres Reference 아님)
DATABASE_URL=
PORTONE_API_SECRET=
JWT_ACCESS_SECRET=
# SESSION_SECRET=  (비우면 JWT와 동일)

# === 권장 ===
REDIS_URL=
GEMINI_API_KEY=
PORTONE_API_KEY=
PORTONE_WEBHOOK_SECRET=
DIRECT_URL=
FILE_STORAGE_PROVIDER=mock
CORS_ORIGIN=https://your-web-service.up.railway.app

# 선택
PORT=8788
NODE_ENV=production
